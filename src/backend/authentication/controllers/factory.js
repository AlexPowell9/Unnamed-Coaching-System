factory = {
    /**
     * create the application, add all routes
     * @param app
     * inject app into factory to allow for different app frameworks
     * @returns app
     * the application
     */
    appFactory: (app) => {
        
        return app;
    }
}